<%- include("../partials/header.ejs") %>

<div class="container-fluid" id="container-fluid"> 
    <div class="container">
        <header class="bg-light p-5 rounded-lg m-3 text-center" id="header">
            <div style="display: flex; align-items: center;">
                <h1 class="header-h1-text">Notifications</h1>
                <p style="cursor: pointer;"><i class="fas fa-chevron-down" id="show_links"></i></p>
                <div style="display: flex; align-items: flex-start; width: 10%;">  
                    <% if(currentUser.referrerDetails.unread_notifications_count > 0) { %> 
                        <p><a href="/referrers/<%=currentUser.username%>/notifications"><i class="fas fa-bell" style="font-size: 2rem; color: #0275D8;"></i></a></p>
                        <span><%= currentUser.referrerDetails.unread_notifications_count %> </span>
                    <% } else { %>
                        <p><a href="/referrers/<%=currentUser.username%>/notifications"><i class="fas fa-bell" style="font-size: 2rem; color: white;"></i></a></p>
                    <% } %> 
                </div>
            </div>
            <%- include("../partials/navbar_referrer.ejs") %>
        </header>

        <br>

        <div class="notifications overflow-auto">

            <% currentUser.referrerDetails.notifications.forEach(function(notification) { %>
                <% var date = new Date(); %> 
                <% var months = ['January','February','March','April','May','June','July', 'August','September','October','November','December']; %>
                <% var today_formatted = date.getDate() + ' ' + months[date.getMonth()] + ' ' + date.getFullYear() %> 
                <% var today = date.getDate() + "/" + (date.getMonth() + 1) + "/" + date.getFullYear() %>  
                <% var timeOrDate = (notification.date === today_formatted) ? notification.time : notification.date %>

                <% function formatTextLength(text, limit = 17) { %> 
                    <% var newText = []; %> 

                    <% if(text.length > limit) { %> 
                        <% text.split(" ").reduce(function(acc, cur) { %>
                            <% if(acc + cur.length <= limit) { %> 
                                <% newText.push(cur); %> 
                            <% } %> 

                            <% return acc + cur.length; %> 
                        <% }, 0); %> 

                        <% return newText.join(" ") + " ..."; %> 
                    <% } %> 

                    <% return text; %> 
                <% } %> 

                <% var btnOutlineClass = notification.status === "unread" ? "btn-outline-primary" : "btn-outline-secondary"; %> 

                <button class="btn notification <%=btnOutlineClass%>" onclick="window.location.href='/referrers/<%=currentUser.username%>/notifications/<%=notification._id%>';">
                    <span class="notification_time"><%= timeOrDate.toUpperCase() %> </span>
                    <span class="notification_content"><%= formatTextLength(notification.content) %> </span>
                </button>

            <% }); %>
            <br><br><br><br><br>
        </div>
    </div>
</div>

<%- include("../partials/footer.ejs") %>